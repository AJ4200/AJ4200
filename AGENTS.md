# Agent Rules

1. Skip build checks unless the user explicitly asks for them.
2. Preserve the site's distinct route colors, fonts, motion, and neon identity.
3. Prefer focused changes that improve the requested page without flattening its personality.
